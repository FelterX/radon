package radon.engine.scenes.components.audio;

import radon.engine.scenes.Scene;
import radon.engine.scenes.components.AbstractComponentManager;

public class AudioPlayerManager extends AbstractComponentManager<AudioPlayer> {

    protected AudioPlayerManager(Scene scene) {
        super(scene);
    }
}
